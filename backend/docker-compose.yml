services:
  postgres:
    image: postgres:18.1
    environment:
      - POSTGRES_USER=alraay
      - POSTGRES_PASSWORD=12343
    volumes:
      - postgres:/var/lib/postgresql
    ports:
      - '5433:5432'

volumes:
  postgres: