services:
  postgres:
    image: postgres:18.1
    container_name: alraay_postgres
    environment:
      - POSTGRES_USER=alraay
      - POSTGRES_PASSWORD=12343
    volumes:
      - alraay_postgres_data:/var/lib/postgresql/data
    ports:
      - '5433:5432'

volumes:
  alraay_postgres_data: